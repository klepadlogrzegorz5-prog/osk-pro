sed -i 's/} else if (showRoleSelection) {/} else if (showPinAuth \&\& showRoleSelection) {\n        PinAuthScreen(\n            onPinSuccess = {\n                showPinAuth = false\n                showRoleSelection = false\n            },\n            onCancel = {\n                viewModel.disablePinLogin()\n                showPinAuth = false\n            },\n            validatePin = { pin -> viewModel.validatePin(pin) }\n        )\n    } else if (showRoleSelection) {/g' app/src/main/java/com/example/MainActivity.kt
