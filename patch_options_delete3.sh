sed -i 's/        if (activeDialogType == ConfigDialogType.PRICING) {/        if (showClearSimulationConfirm) {\n            AlertDialog(\n                onDismissRequest = { showClearSimulationConfirm = false },\n                containerColor = Color(0xFF1E293B),\n                title = { Text("Potwierdzenie Oczyszczenia", color = Color.White) },\n                text = { Text("Czy na pewno chcesz usunąć CAŁĄ symulację (wszystkie pojazdy, instruktorów, kursantów, finanse i rezerwacje)? Aplikacja powróci do czystego stanu komercyjnego. Operacji nie można cofnąć.", color = Color(0xFF94A3B8)) },\n                confirmButton = {\n                    TextButton(\n                        onClick = {\n                            viewModel.clearSimulationData()\n                            showClearSimulationConfirm = false\n                        }\n                    ) {\n                        Text("Tak, wyczyść aplikację", color = Color(0xFFEF4444))\n                    }\n                },\n                dismissButton = {\n                    TextButton(onClick = { showClearSimulationConfirm = false }) {\n                        Text("Anuluj", color = Color.White)\n                    }\n                }\n            )\n        }\n\n        if (activeDialogType == ConfigDialogType.PRICING) {/g' app/src/main/java/com/example/ui/screens/OptionsScreen.kt
