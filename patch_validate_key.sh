sed -i 's/fun validateUserKey(key: String, role: UserRole): String? {/fun validateUserKey(name: String, key: String, role: UserRole): String? {\n        val validKey = userKeys.value.find { it.code == key \&\& it.role == role.label \&\& it.assignedName.equals(name.trim(), ignoreCase = true) }\n        return validKey?.assignedName\n    }\n\n    fun oldValidateUserKey(key: String, role: UserRole): String? {/g' app/src/main/java/com/example/ui/viewmodel/OskViewModel.kt
