sed -i 's/\/\/ Footer info/\/\/ Footer info\n            Row(\n                modifier = Modifier\n                    .align(Alignment.CenterHorizontally)\n                    .clip(RoundedCornerShape(8.dp))\n                    .clickable { showCreatorDialog = true }\n                    .padding(horizontal = 12.dp, vertical = 6.dp),\n                verticalAlignment = Alignment.CenterVertically,\n                horizontalArrangement = Arrangement.Center\n            ) {\n                Icon(\n                    imageVector = Icons.Default.Info,\n                    contentDescription = "O twórcy",\n                    tint = Color(0xFF38BDF8).copy(alpha = 0.7f),\n                    modifier = Modifier.size(14.dp)\n                )\n                Spacer(modifier = Modifier.width(6.dp))\n                Column(horizontalAlignment = Alignment.CenterHorizontally) {\n                    Text(\n                        text = "OSK-PRO v1.0 • Cyberpunk Edition",\n                        fontSize = 10.sp,\n                        fontWeight = FontWeight.Bold,\n                        color = Color.White.copy(alpha = 0.4f)\n                    )\n                    Text(\n                        text = "GrzesKlep secure your app 24\/h",\n                        fontSize = 9.sp,\n                        fontWeight = FontWeight.Bold,\n                        color = Color(0xFF38BDF8).copy(alpha = 0.7f)\n                    )\n                }\n            }\n            if (showCreatorDialog) {\n                CreatorInfoDialog(onDismiss = { showCreatorDialog = false })\n            }/g' app/src/main/java/com/example/ui/components/AppDrawer.kt
