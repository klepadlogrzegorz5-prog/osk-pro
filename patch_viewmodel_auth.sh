sed -i 's/    private val _userName = MutableStateFlow("Jan Kowalski")/    private val _userName = MutableStateFlow("Jan Kowalski")\n    fun setUserName(name: String) { _userName.value = name }\n\n    fun validateUserKey(key: String, role: UserRole): String? {\n        val validKey = userKeys.value.find { it.code == key \&\& it.role == role.label }\n        return validKey?.assignedName\n    }/g' app/src/main/java/com/example/ui/viewmodel/OskViewModel.kt
